const help = () => {
  console.log('login to an account using the command `/login`');
  console.log('logout of an account using the command `/logout`');
  console.log('make a new account using the command `/register`');
};

module.exports = () => {
  help();
};
